{
  "name": "app",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "app:develop": "cd packages/app/ && yarn start",
    "backend:develop": "cd packages/backend/ && yarn develop",
    "prestart": "cd packages/backend/ && yarn build",
    "start": "node packages/backend/dist/"
  }
}
