{
  "name": "app",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "heroku-postbuild": "yarn workspaces run build",
    "app:develop": "cd packages/app/ && yarn start",
    "backend:develop": "cd packages/backend/ && yarn develop"
  }
}
